# Maintainer: Jan Alexander Steffens (heftig) <heftig@archlinux.org>

pkgname=egl-wayland2
pkgver=1.0.1
pkgrel=1
_commit=3e5b643542708cbb9c67d18fe19b5fa4151d7d8f
pkgdesc="new EGLStream-based Wayland external platform"
url="https://github.com/NVIDIA/egl-wayland2"
arch=(x86_64)
license=(MIT)
depends=(
  eglexternalplatform
  glibc
  libdrm
  wayland
)
makedepends=(
  git
  'libglvnd>=1.3.4'
  meson
  wayland-protocols
)
provides=(libnvidia-egl-wayland2.so)
source=("git+https://github.com/NVIDIA/egl-wayland2#commit=$_commit")
sha256sums=('45fa88b0d91abdcc8d942f1a8a74fba8a2a0f5cac317b941550f4d7a2a360a1b')

prepare() {
  cd $pkgname
}

build() {
  arch-meson $pkgname build
  meson compile -C build
}

check() {
  meson test -C build --print-errorlogs
}

package() {
  meson install -C build --destdir "$pkgdir"
}
