# Maintainer: Peter Jung <admin@ptr1337.dev>

pkgname=mysettings
pkgver=1.0.0
pkgrel=1
arch=('any')
license=(GPL-1.0-only)
makedepends=('coreutils' 'git')
pkgdesc='CachyOS - Settings'
source=("git+https://github.com/VolRencs/mysettings.git#tag=$pkgver")
install="$pkgname.install"
sha256sums=('SKIP')
depends=(
  zram-generator
  inxi
  "systemd>=256"
)
provides=('ntsync-common')
prepare() {
  cd $_gitname
}
package() {
  install -d "$pkgdir/etc"
  cp -rf "${srcdir}/CachyOS-Settings/etc" "$pkgdir"
  install -d "$pkgdir/usr"
  cp -rf "${srcdir}/CachyOS-Settings/usr" "$pkgdir"
}
